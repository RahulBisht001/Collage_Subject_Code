import React from "react";

const StudentName = ({stdName}) => {
    return (
        <div>
            <h2>Give Student Name is : {stdName}</h2>
        </div>
    );
};

export default StudentName;
